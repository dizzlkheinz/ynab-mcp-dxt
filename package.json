{
  "name": "ynab-mcp-server",
  "version": "0.4.1",
  "description": "Model Context Protocol server for YNAB (You Need A Budget) integration",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "prebuild": "npm run clean && npm run format && npm run lint",
    "build": "tsc && npm run bundle",
    "build:prod": "npm run prebuild && tsc --project tsconfig.prod.json && npm run bundle:prod && npm run verify-build",
    "clean": "rimraf dist",
    "dev": "tsc --watch",
    "start": "node -r dotenv/config dist/index.js",
    "start:prod": "NODE_ENV=production node dist/index.js",
    "start:mcp": "node dist/index.js",
    "validate-env": "node scripts/validate-env.js",
    "verify-build": "node scripts/verify-build.js",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run --project unit",
    "test:integration": "vitest run --project integration",
    "test:e2e": "vitest run --project e2e",
    "test:coverage": "vitest run --coverage",
    "test:performance": "vitest run src/__tests__/performance.test.ts",
    "test:comprehensive": "tsx src/__tests__/testRunner.ts",
    "test:all": "npm run test:unit && npm run test:integration && npm run test:e2e && npm run test:performance",
    "generate:dxt": "powershell -ExecutionPolicy Bypass -File scripts/generate-dxt.ps1",
    "bundle": "esbuild dist/index.js --bundle --platform=node --format=cjs --outfile=dist/bundle/index.cjs --sourcemap",
    "bundle:prod": "esbuild dist/index.js --bundle --platform=node --format=cjs --outfile=dist/bundle/index.cjs --minify",
    "package:dxt": "npm run build:prod && npm run generate:dxt",
    "prepare": "npm run build",
    "prepublishOnly": "npm run test:all && npm run build:prod"
  },
  "keywords": [
    "mcp",
    "ynab",
    "budget",
    "finance"
  ],
  "author": "",
  "license": "AGPL-3.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.1",
    "csv-parse": "^6.1.0",
    "d3-array": "^3.2.4",
    "date-fns": "^4.1.0",
    "dotenv": "^17.2.1",
    "ynab": "^2.9.0",
    "zod": "^4.0.15"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/d3-array": "^3.2.1",
    "@types/node": "^22.10.2",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "esbuild": "^0.23.0",
    "eslint": "^9.35.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "rimraf": "^5.0.10",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.42.0",
    "vitest": "^3.2.4"
  }
}
